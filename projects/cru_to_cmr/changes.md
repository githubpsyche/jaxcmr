Page 1, Abstract, in 2: change "context maintenance and retrieval" to to "Context Maintenance and Retrieval". 

Page 1, Abstract, in 3: change "context retrieval and updating" to "Context Retrieval and Updating"

Page 2, Paragraph 2, in 1: change "temporal context model" to "Temporal Context Model".

Page 2, Paragraph 2, in 3: change "context maintenance and retrieval" to to "Context Maintenance and Retrieval". 

Page 2, paragraph 3, in 3: change "context retrieval and updating" to "Context Retrieval and Updating"

Page 3, Table 1, in Note: change "context maintenance and retrieval" to to "Context Maintenance and Retrieval". Change "context retrieval and updating" to "Context Retrieval and Updating".

Page 4, equation 2: Change the subscript from 1 to i in the term c₁ᴵᴺ — so it should read cᵢᴵᴺ, with the same bold arrow on top and same superscript "IN" below but a lowercase "i" as the subscript. In latex, that's a change from $\mathbf{\vec{c_{1}^{\text{IN}}}}$ to $\mathbf{\vec{c_{i}^{\text{IN}}}}$.

Page 4, first sentence after equation 2: Change the subscript from 1 to i in the term c₁ᴵᴺ — so it should read cᵢᴵᴺ, with the same bold arrow on top and same superscript "IN" below but a lowercase "i" as the subscript. In latex, that's a change from $\mathbf{\vec{c_{1}^{\text{IN}}}}$ to $\mathbf{\vec{c_{i}^{\text{IN}}}}$.

Page 4, equation 3: Change the subscript from 1 to i in both occurrences of the term term c₁ᴵᴺ — so it should read cᵢᴵᴺ, with the same bold arrow on top and same superscript "IN" below but a lowercase "i" as the subscript. In latex, that's a change from $\mathbf{\vec{c_{1}^{\text{IN}}}}$ to $\mathbf{\vec{c_{i}^{\text{IN}}}}$.

Page 4, section "Contextual Input: CRU Omits Feature-to-Context Learning", paragraph 1, sentence 2: Change the subscript from 1 to i in the term c₁ᴵᴺ and remove the arrow across *all* occurrencs of "c". For example, it should read cᵢᴵᴺ, with bold arrow on top removed, same superscript "IN" below, and a lowercase "i" as the subscript. 

Page 5, paragraph 4, sentence 4: Change the subscript from 1 to i in the term c₁ᴵᴺ and remove the arrow across *all* occurrencs of "c". For example, it should read cᵢᴵᴺ, with bold arrow on top removed, same superscript "IN" below, and a lowercase "i" as the subscript. 

Page 6, section "Serial Position Effects", equation 2: change M^{FC} to M^{CF}.

Page 7, paragraph 2, sentence 1, Change "In either model, retrieval" to "Retrieval".

Page 7, paragraph 2, sentence 6, Change, "In either model, once" to "Once".
